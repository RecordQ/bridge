// This is a placeholder file. The logic for authentication is handled in middleware.
// In a real application, you might use this layout for admin-specific UI.
import { type ReactNode } from "react";

export default function AdminLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
